#ifndef COURSE_H
#define COURSE_H

#include <string>
#include "Student.h"
using namespace std;
course class{
  private:
          string CourseCode;
          String CourseName;
          int MaxStudents;
          Student* Students[];
          int CurrentStudents;
          addStudent();
          DisplayCourseInfo();
 public:
         course addstudent::Student(const student& s){
           this student -> student[];
           student[] = new student ; 
}
      Display CourseCode(string CourseCode){
      this CourseCode -> CourseCoded;
      return CourseCode;
}
    Display CourseName(string CourseName){
    this CoursesName -> CourseName;
    return CourseName;
}
    DisplayCourseInfo(string CourseCode , string  CourseName , int  MaxStudents , int CurrentStudents){
    this MaxStudents -> MaxStudents; 
    this CurrentStudents -> CurrentStudents;
    cout << "The CourseCode is : " << CourseCode;
    cout << "The CourseName is : " << CourseName;
    cout << " The Number of Max Stuents is : " << MaxStudents :
    cout << "The number of current Students is : " << CurrentStudents;\
}
};
    
           













#endif
